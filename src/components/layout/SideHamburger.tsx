'use client';

/**
 * @deprecated This component is no longer used.
 * Layout is now handled by (authRequire)/layout.tsx with Sidebar + TopNav.
 */
export const SideHamburger = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default SideHamburger;
